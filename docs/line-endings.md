# Line Endings Policy

This repository now defines line-ending rules in `.gitattributes`.

We intentionally did **not** run `git add --renormalize .` in this change.

If the team approves normalization later, run:

```bash
git add --renormalize .
```

in a dedicated PR.
